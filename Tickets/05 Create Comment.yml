info:
  name: Create Ticket Comment
  type: http
  seq: 5
http:
  method: POST
  url: "{{ticketBaseUrl}}/tickets/1001/comments"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "note": "Investigating the issue"
      }
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
