info:
  name: Get Ticket Comments
  type: http
  seq: 6
http:
  method: GET
  url: "{{ticketBaseUrl}}/tickets/TIC83927462/comments?cursor=0&limit=5"
  params:
    - name: cursor
      value: "0"
      type: query
    - name: limit
      value: "5"
      type: query
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
