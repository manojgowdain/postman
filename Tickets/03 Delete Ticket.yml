info:
  name: Delete Ticket
  type: http
  seq: 3
http:
  method: DELETE
  url: "{{ticketBaseUrl}}/tickets/12345"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
