info:
  name: Reset Password
  type: http
  seq: 10
http:
  method: POST
  url: "{{apiBaseUrl}}/users/reset-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "resetToken": "YOUR_RESET_TOKEN",
        "newPassword": "Manoj@2002"
      }
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
