info:
  name: Get Health History
  type: http
  seq: 4
http:
  method: GET
  url: "{{apiBaseUrl}}/users/health/history/DEVICE123"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
