info:
  name: Logout Current Session
  type: http
  seq: 4
http:
  method: POST
  url: "{{apiBaseUrl}}/staff/logout"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
