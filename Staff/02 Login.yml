info:
  name: Staff Login
  type: http
  seq: 2
http:
  method: POST
  url: "{{apiBaseUrl}}/staff/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "identifier": "9876543210",
        "password": "Admin@123"
      }
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
