info:
  name: Verify Session
  type: http
  seq: 3
http:
  method: POST
  url: "{{apiBaseUrl}}/staff/verify-session"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
