info:
  name: Get Staff Profile
  type: http
  seq: 11
http:
  method: GET
  url: "{{apiBaseUrl}}/staff/profile/STAFF-ID-HERE"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
