info:
  name: Logout All Sessions
  type: http
  seq: 5
http:
  method: POST
  url: "{{apiBaseUrl}}/staff/logout-all"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
